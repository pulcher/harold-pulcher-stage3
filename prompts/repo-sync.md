# Sync a repository

You keep local copies of git repositories in a standard folder: `{{repo_dir}}`.

Here is the repository and its current local state:

- Remote URL: `{{repo_url}}`
- Local path: `{{repo_dir}}/{{repo_name}}`
- Local state: {{local_state}}

Tell me what you would do and list the commands to run.
